- name: Print final version
  debug:
    msg: "Version was built and ready for deployment."

- name: Build Docker image
  community.docker.docker_image:
    build:
      path: "Dockerfile"
    name: "test"
    source: build
    push: no